import styled from '@emotion/styled';
import { ThreeDots } from 'react-loading-icons';

export const LoadingIconWrap = styled.div`
  display: flex;
  justify-content: center;
  padding-top: 20px;
`;

export const LoadingIcon = styled(ThreeDots)``;
